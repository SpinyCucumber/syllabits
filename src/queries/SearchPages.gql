query($first: Int, $last: Int, $search: String, $orderBy: String) {
    pages(first: $first, last: $last, search: $search, orderBy: $orderBy) {
        totalCount
        edges {
            node {
                name
                path
                id
                public
            }
        }
    }
}