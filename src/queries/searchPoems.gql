query($first: Int, $last: Int, $search: String) {
    allPoems(first: $first, last: $last, search: $search) {
        totalCount
        edges {
            node {
                title
                author
                id
            }
        }
    }
}