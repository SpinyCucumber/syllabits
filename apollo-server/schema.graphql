"Included scalars"
scalar JSON
scalar Upload

interface Node {
  id: ID!
}

type Line {
  number: Int!
  stanza: Int
  text: String!
}

"TODO Add support for connections"
type Poem implements Node {
  id: ID!
  name: String!
  author: String
  lines: [Line]!
}

type Query {
  randomPoem: Poem!
  node(id: ID!): Node
}
