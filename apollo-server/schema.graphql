"Light-weight node definition"
interface Node {
  id: ID!
}

type Line {
  number: Int!
  stanza: Int
  text: String!
}

type Poem implements Node {
  id: ID!
  name: String!
  author: String
  lines: [Line]!
}

type Query {
  randomPoem: Poem!
  node(id: ID!): Node
}
