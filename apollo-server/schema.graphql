"Included scalars"
scalar JSON
scalar Upload

type Line {
  number: Int!
  stanza: Int
  text: String!
}

"TODO Add support for connections"
type Poem {
  name: String!
  author: String
  lines: [Line]!
}

type Query {
  randomPoem: Poem!
}
